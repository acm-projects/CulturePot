import 'package:flutter/material.dart';

class MySearchPref extends StatelessWidget {
  const MySearchPref({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}